# 最终提交清单

## 本地已完成

- [x] 仓库内建立 `3124004070` 学号文件夹
- [x] Java 8 兼容的论文查重程序
- [x] 三个绝对路径的命令行接口
- [x] 答案保留两位小数
- [x] 21 个 JUnit 测试
- [x] JaCoCo 行覆盖率 96.23%、分支覆盖率 85.71%
- [x] Checkstyle 0 项违规
- [x] SpotBugs 0 错误、0 警告
- [x] JProfiler 优化前后原始快照
- [x] PSP 预估和实际耗时
- [x] 博客园正文草稿
- [x] Java 8 字节码的 `main.jar`

## 提交前由本人完成

1. 从两份 `.jps` 快照截取 JProfiler Hot Spots 图片，放到 `docs/images`，并插入博客性能章节。
2. 检查 PSP 实际耗时是否与本人记录一致；如有差异，以本人真实时间为准修改。
3. 在仓库根目录执行 `git push -u origin main`。
4. 在 GitHub 创建 `v1.0.0` Release，上传作业目录根部的 `main.jar`。
5. 将 `blog/第一次个人编程作业.md` 发布到博客园，上传三张截图并更新图片地址。
6. 确认博客正文第一行仍是 GitHub 仓库链接，博客标题为“第一次个人编程作业”。
7. 在另一目录使用绝对路径执行一次 Release 中下载的 JAR，确认输出答案文件。

## 最终运行命令

```text
java -jar main.jar C:\tests\orig.txt C:\tests\orig_add.txt C:\tests\ans.txt
```
